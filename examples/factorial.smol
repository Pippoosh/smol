; Factorial of a non-negative integer
;
; 1. read n
; 2. start acc at 1
; 3. if n <= 1, jump to the print
; 4. acc = acc * n
; 5. n = n - 1
; 6. loop back to 3
; 7. print acc

( let n <-> )
( let acc 1 )
( if ( <= n 1 ) ( goto 7 ) )
( let acc ( * acc n ) )
( let n ( - n 1 ) )
( goto 3 )
acc
